interface:
  display_name: "Orchestrate"
  short_description: "Session mode: delegate all work to sub-agents and act only as the user's interface"
  default_prompt: "Use $orchestrate to switch this session into orchestrator mode: delegate all research, implementation, and verification to sub-agents and report back to me."
policy:
  allow_implicit_invocation: false
